```
1 # 1.注释
  2 # 单行注释  航前加#
  3 # 多行注释 ：
  4 :<<EOF
  5 多行注释1
  6 多行注释2
  7 ...
  8 
  9 EOF
 10 
 11 :<<+
 12 多行注释1
 13 多行注释2
 14 ...
 15 +
 16 
 17 # 2. 变量
 18 :<<+
 19 1.环境变量（全局变量），系统声明好的，例如：PATH
 20 2.shell变量，由系统已经声明好的变量，=有特殊的功能和意义
 21 3.由程序员自己声明再shell中的变量，这个是局部变量（局部指的是只能在当前程序中使用；全局实在所有的程序中使用）
 22 
 23 语法：变量名=值   - - 注意：=两边不能有空格；变量名和python的要求一样
 24 +
 25 stu_name="张三"
 26 age=10
 27 #2使用变量的值：
 28 # 语法1：$变量名
 29 # name=$stu_name
 30 # echo $name
 31 # 语法2：${变量名}   -- 推荐使用
 32 name2=${stu_name}
 33 echo ${name2}is student
 34 
 35 stu_name='李四'  # 修改变量的值
 36 echo $stu_name
 37 
 38 # 2.3只读变量
 39 # redonly 变量名
 40 gender='女'
 41 readonly gender
 42 gender='男'
 43 echo $gender
 44 
 45 # 2.4删除变量
 46 # unset 变量名
 47 unset age
 48 echo ===${age}===
 49 
 50 # 3.获取Linux指令结果
 51 # 在shell 脚本中直接写Linux指令，可以直接执行
 52 cd /home/shell
 53 ls
 54 
 55 #获取Linux指令结果
 56 #变量=$(指令)
 57 # 变量=`指令`
 58 echo ===========
 59 list=`ls`
 60 echo 执行ls后的结果是：${list}===
 61 
 62 # 查找子字符串
 63 # 查找字符j 或 o 的位置(哪个字母先出现就计算哪个)
 64 string='runood jnk hhh'
 65 echo $(expr index "$string" oj)

```

```
1 # 1.字符串
  2 # shell 脚本中双引号单引号没有引号，都表示字符串
  3 name=jnk
  4 name2="jnk"
  5 name3='jnk'
  6 echo ${name} ${name2} ${name3}
  7 
  8 # 1.1获取字符/字串
  9 #语法：字符串:下标:个数    -- 从字符产中从指定下标开始获取指定个数的字符，返回一个新的字符串
 10 # 字符产:下标:1    --- 获取字符串中指定下标对应的字符
 11 # 字符串:下标  --  从指定下标获取到字符产结尾
 12 
 13 str1='hellorel'
 14 echo ${str1:3:2} # lo
 15 echo ${str1:5:1} # r
 16 echo ${str1:5}  # rel
 17 
 18 #1.2 获取字符串的长度：
 19 # 语法：  ${#字符串变量}
 20 echo ${#str1}  # 8
 21 
 22 # 只有 双引号字符串中可以出现变量和转义字符
 23 aaa="abc\n123"
 24 echo ${aaa}
 25 echo ${#aaa}
 26 echo -e ${aaa}  # echo -e  -- 打印的时候转义字符有效
 27 
 28 # 2.数组
 29 #bash中支支持一维数组，（数组中不能出现数组）
 30 
 31 # 语法：（元素1 元素2 元素3 ...）  --- 由小括号括起来，元素之间由空格隔开
 32 
 33 arr1=(100jnk 你好 '20' "hello")
 34 echo ${arr1}   # 100jnk   直接获取的是这个数组第一个元素的值
 35 echo ${arr1:1} # 00jnk
 36 
 37 # 2.1 获取数组元素
 38 # 语法：数组[下标]
 39 a1=${arr1[1]}
 40 echo ${a1}  # 你好    --获取数组中指定下标对应的元素
 41 # 数组[@]  / 数组[*]    -- 获取数组中所有的元素
 42 a2=${arr1[*]}
 43 a3=${arr1[@]}
 44 echo ${a3}
 45 echo ${a2}
 46 
 47 # 2.2获取数组的长度
 48 # 语法：${#数组[@]}  / ${#数组[*]}   --- 获取数组中所有元素的个数
 49 # ${#数组[下标]}  -- 获取数组中指定下标元素的长度

```

```
 # shell中支持：算数运算符，关系运算符，布尔运算符，字符串运算符
  2 
  3 # 1.算术运算符
  4 #  语法：`expr 运算表达式`
  5 rel=10+20
  6 rel2=`expr 10 + 20`
  7 echo ${rel}  #  10+20
  8 echo ${rel2}
  9 echo 求差：`expr 10 - 20`
 10 echo 求商：`expr 100 / 20`
 11 rel3=`expr 10 \* 20`    # 注意：使用*作为乘法运算的时候前加\
 12 echo 求积：${rel3}
 13 echo 求余：`expr 10 % 3`
 14 
 15 
 16 #除法运算
 17 a1=`echo "scale=2;9.0 / 2.0" | bc`
 18 echo ${a1}   # 4.50
 19 
 20 #   涉及小数的加减乘除都参看除法运算的格式
 21 
 22 # 2.关系运算符：比较的是数字的大小
 23 :<<+
 24 -eq   -- 等于，相当于 ==
 25 -nq   -- 不等于,相当于 !=
 26 -gt   -- 大于
 27 -lt   -- 小于
 28 -ge   -- 大于等于
 29 -le   -- 小于等于
 30 使用语法：[ 值1 关系运算符 值2 ]
 31 +
 32 
 33 # 3布尔运算符（逻辑运算符）
 34 # !  --  逻辑非运算
 35 # -o  -- 逻辑或运算
 36 # -a  -- 逻辑与运算
 37 
 38 # read的用法：read 变量名  -- 输入内容，并且将结果保存在变量中
 39 
 40 
 41 
 42 #  判断年份是不是闰年？？？
 43 
 44 printf 请输入年：
 45 read year
 46 echo ${year}
 47 
 48 if [ `expr $year % 4` == 0 -a `expr $year % 100` != 0 -o `expr $year % 400` == 0 ]
 49 then
 50     echo ${year}是闰年！
 51 else
 52     echo ${year}不是闰年
 53 fi
 54 
 55 
 56 # 4.字符串运算符
 57 # [ 字符串1 = 字符串2 ] --  判断两个字符串是否相等
 58 # [ 字符串1 != 字符产2 ]    -- 不相等
 59
```

```
# 1.if语句
  2 :<<+
  3 语法1：
  4 if 条件语句
  5 then 
  6     满足条件执行的代码
  7 fi
  8 
  9 语法2:
 10 if 条件语句
 11 then 
 12    满足条件执行的语句
 13 else
 14     不满足条件执行的语句
 15 fi
 16 
 17 语法3：
 18 if 条件语句1
 19 then 
 20     代码块1
 21 elif 条件语句2
 22 then
 23     代码块2
 24 else
 25     代码块3
 26 fi
 27 +
 28 
 29 #for 循环
 30 :<<+
 31 for 变量 in 序列
 32 do
 33     循环体
 34 done
 35 
 36 +
 37 
 38 # 遍历数组
 39 arr1=(10 20 "jnk")
 40 for x in ${arr1[@]}
 41 do
 42     echo 循环体2：${x}
 43 done
 44 
 45 # 3.while 循环
 46 :<<+
 47 语法：
 48 while 条件语句
 49 do
 50     循环体
 51 done
 52 +
 53 
 54 # 遍历字符串：
 55 
 56 str1="helloworld"
 57 index=0
 58 len=${#str1}
 59 while [ ${index} -lt ${len} ]
 60 do
 61     echo ${str1:${index}:1}
 62     index=`expr ${index} + 1`
 63 done
 64 
 65 # 求 1 + 2 +3 +... 100的和
 66 
 67 num=0
 67 num=0
 68 sum=0
 69 while [ ${num} -le 100 ]
 70 do
 71     sum=`expr ${num} + ${sum}`
 72     num=`expr ${num} + 1`
 73 
 74 done
 75     echo 1+2+3+...+100的和为： ${sum}
 76 
 77 # 死循环
 78 # 语法：while true   /  while :
 79 
 80 while :
 81 do
 82     read -p "请输入数字：" num
 83     if [ ${num} == 100 ]
 84     then
 85         break
 86     fi
 87 done

```

```
1 #1.函数的声明
  2 :<<+
  3 语法：
  4 函数名(){
  5 
  6     函数体
  7     [return 返回值]
  8 }
  9 +
 10 
 11 func1(){
 12 
 13 echo 函数
 14 }
 15 func1   # 调用函数：函数名
 16 
 17 # 2.由参数的函数
 18 # 声明的时候不需要形参，直接在函数题中通过'${N}'来获取第N个实参
 19 #调用的时候：函数名 实参1 实参2 实参3 ...
 20 func2(){
 21 
 22     echo 函数2被调用
 23     echo 第一个：${1}
 24     echo 第二个：${2}
 25     echo 第三个：${3}
 26 }
 27 func2 10 20 "jnk"
 28 
 29 # return 数字   --  数字范围是0~255
 30 func3(){
 31 
 32     echo 函数3被调用
 33     return 10
 34 }
 35 func3
 36 echo return返回的值： ${?}
 37 
```





